A record's read-only details.

```jsx
<DescriptionList items={[['Plan', 'Pro annual'], ['Renews', '01 Sep 2026'], ['Card', 'Visa ···· 4242']]} />
<DescriptionList stack items={[['Status', <Badge tone="success">Active</Badge>]]} />
```

`stack` for sidebars and drawers; the default two-column for modals and card bodies.
