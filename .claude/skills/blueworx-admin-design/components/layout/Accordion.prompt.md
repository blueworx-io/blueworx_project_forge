Optional or advanced settings that would otherwise bury the common ones.

```jsx
<Accordion title="Advanced" subtitle="Webhooks, cron and debug logging" aside={<Badge tone="neutral">3</Badge>}>
  <div className="bw-fields">…</div>
</Accordion>
```

Closed by default. Never hide a required field inside one.
