Top-level views of one screen — never for navigation between screens (that is the WordPress submenu's job).

```jsx
<Tabs value={tab} onChange={setTab} items={[{ id: 'all', label: 'All', count: 128 }, { id: 'active', label: 'Active', count: 96 }]} />
```
