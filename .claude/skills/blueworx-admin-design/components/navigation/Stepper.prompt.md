Setup and import flows. Use instead of Tabs when the steps have an order.

```jsx
<Stepper current={step} onChange={setStep}
  steps={[{ id: 'basics', label: 'Basics' }, { id: 'look', label: 'Look' }, { id: 'finish', label: 'Finish' }]} />
```
