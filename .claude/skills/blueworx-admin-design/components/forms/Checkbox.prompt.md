A single opt-in, or a list of independent options.

```jsx
<Checkbox label="Email me when a payment fails" defaultChecked />
<Checkbox label="Delete data on uninstall" help="Cannot be undone." />
```

For an on/off setting that reads as a state rather than a choice, use Switch.
