Native select, styled. Use for 4+ mutually exclusive options; use RadioGroup for 2–3.

```jsx
<Select placeholder="All statuses" options={['Active','Paused','Cancelled']} />
<Select options={[{ value: 'admin', label: 'Administrator' }, { value: 'editor', label: 'Editor' }]} />
```
