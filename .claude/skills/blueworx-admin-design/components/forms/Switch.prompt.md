On/off for a capability or visibility setting — the state, not a choice.

```jsx
<Switch label="Show the reports page" defaultChecked />
<Switch bare aria-label="Enable module" />
```

`bare` drops the pill wrapper for table cells and dense rows.
