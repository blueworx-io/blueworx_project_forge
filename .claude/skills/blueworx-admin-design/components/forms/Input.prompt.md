Text, email, number, URL and search inputs.

```jsx
<Input placeholder="Search orders" icon="search" />
<Input mono defaultValue="[bw_members]" />
<Input type="number" affix="days" defaultValue={30} />
```

`mono` for anything code-shaped; `icon` for search and key fields; `invalid` pairs with a Field `error`.
