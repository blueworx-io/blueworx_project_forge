Longer prose or snippets. Vertical resize only — never a fixed unresizable box.

```jsx
<Textarea rows={5} placeholder="Message shown to members after checkout" />
<Textarea mono rows={6} defaultValue="add_filter('bw_admin_menu', ...);" />
```
