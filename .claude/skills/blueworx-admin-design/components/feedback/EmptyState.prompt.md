First-run state of a table or panel. Say what will appear here and give one action.

```jsx
<EmptyState icon="shopping-cart" title="No orders yet" actions={<Button variant="primary" icon="plus">Add a product</Button>}>
  Orders appear here as soon as your first checkout completes.
</EmptyState>
```
