Short label on hover for a control whose meaning is not obvious. IconButton already has one via `label`.

```jsx
<Tooltip label="Synced 4 minutes ago"><Badge tone="success" dot>Live</Badge></Tooltip>
```
