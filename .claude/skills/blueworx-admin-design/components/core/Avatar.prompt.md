User marks in tables, drawers and comment threads. `Person` is the table-cell pairing.

```jsx
<Avatar name="Priya Raman" size={28} />
<Person name="Priya Raman" secondary="priya@example.com" />
```

Falls back to initials on the brand wash when there is no gravatar.
