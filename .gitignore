node_modules/
dist/
dist-zip/

# Composer dev tooling (PHPCS). Nothing here is runtime.
/vendor/
composer.lock
.phpunit.result.cache

# Playwright
/test-results/
/playwright-report/
playwright-report.json
/blob-report/
/playwright/.cache/

# Disposable local WordPress test instance (foundation wp-test-env.mjs)
.wp-test/

# The deployable zip is built one level up, never in the tree.
*.zip

*.log
.DS_Store
Thumbs.db
.superpowers/
.playwright-mcp/

# The client half of the two-instance test harness (#86): a disposable
# WordPress and the staged client plugin it links.
.wp-test-client/
.wp-test-client-plugin/
playwright-pair-report.json

# The shared foundation, checked out beside this repo by CI.
.foundation/
